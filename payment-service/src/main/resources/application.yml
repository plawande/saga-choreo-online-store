spring:
  application:
    name: payment-service
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  datasource:
    url: jdbc:mysql://localhost:3306/choreostorepaymentdb
    username: root
    password: Test1234
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: rabbit$_$123

---

server:
  port: 8082

---

logging:
  level:
    com.sample.payment: debug

---

queue:
  order-create: ORDER_CREATED_QUEUE
  billed-order: BILLED_ORDER_QUEUE
  order-cancel: ORDER_CANCELED_QUEUE
  refund-payment: REFUND_PAYMENT_QUEUE